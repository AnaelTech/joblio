---
interface Props {
  message?: string;
}

const { message } = Astro.props;
---

{message && (
  <div class="mt-4 rounded-lg border border-red-500/20 bg-red-500/10 px-4 py-3 text-sm text-red-600">
    {message}
  </div>
)}
